# TODO

- [x] Inspect existing repo gitignore situation.
- [x] Create root `/.gitignore` with common Python/Venv/secret/data ignore rules for pushing.
- [x] Check ignore behavior (venv/ is ignored by root .gitignore).
- [x] Confirm repo is only showing untracked: root .gitignore itself (if not staged) and TODO.md.


